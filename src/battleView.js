class BattleView {
  constructor(game, ctx, playerCreature, enemyCreature) {
    this.ctx = ctx;
    this.game = game;
    this.playerCreature = playerCreature;
    this.enemyCreature = enemyCreature;
  }
}